version: 2

sources:
  - name: source
    description: a clone of a Postgres 
    database: postgres
    schema: postgres
    tables:
      - name: customers_dataset
      - name: web_forms
      - name: agents_dataset
      - name: call_center_logs
      - name: media_complaints
